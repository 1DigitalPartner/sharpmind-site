{
  "name": "sharpmind-site",
  "version": "1.0.0",
  "description": "SharpMind — AI Insights & Analytics (static, auto-generated)",
  "scripts": {
    "stage": "rm -rf build && mkdir -p build/en && mkdir -p build/assets && cp -R public/* build/ || true",
    "build": "npm run stage && node scripts/build_site.mjs",
    "deploy": "wrangler pages deploy build --project-name sharpmind --commit-dirty=true"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
